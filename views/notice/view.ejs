<!DOCTYPE html>
<html>
  <head>
    <% include ../partials/head %>
    <style>
      .side{
        flex : 2;
      }
      .title{
        flex : 8;
      }
      .info{
        flex : 2;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <% include ../partials/header %>
    </div>
    <div class="container">
      <div class="container row" style="margin-top:30px">
        <div class="panel panel-default">
          <div class="panel-heading" style="display:flex;height:50px">
            <div>
              <font size="4"><b><%= notices[1]%></b></font>
            </div>
          </div>
          <div class="panel-body">
            <div class="top" style="display:flex;height:50px">
              <div class="info">
                <font size="2">작성자 : <%= notices[5]%></font>
              </div>
              <div class="info">
                <font size="2">이메일 : <%= notices[6]%></font>
              </div>
              <div class="info">
                <font size="2">작성일 : <%= notices[4].toString().substr(0,16) %></font>
              </div>
            </div>
            <div>
              <p style="white-space:pre-line"><%= notices[2] %></p>
            </div>
          </div>
        </div>
      </div>
      <div class="row" style="margin-top:0px" align="right">
      <% if (iswriter) { %>
        <input type="button" name="add" id="add" value="수정" class="btn btn-primary pull" style="margin-right:5px" onclick="location.href='/notice/modify/<%= notices[0] %>'">
        <input type="button" name="add" id="add" value="삭제" class="btn btn-primary pull" style="margin-right:5px" onclick="location.href='/notice/delete/<%= notices[0] %>'">
      <% }  %>
        <input type="button" style="margin-right:15px" value="목록" class="btn btn-primary pull" onclick="location.href='/notice'">
      </div> 
    </div>
  </body>
</html>
