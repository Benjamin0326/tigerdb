<!DOCTYPE html>
<html>
  <head>
    <% include ../partials/head %>
    <style>
      .side{
        flex : 2;
      }
      .title{
        flex : 8;
      }
      .info{
        flex : 2;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <% include ../partials/header %>
    </div>
    <div class="container">
      <div class="container row" style="margin-top:30px">
        <div class="panel panel-default">
          <div class="panel-heading" style="display:flex;height:50px">
            <div>
              <font size="4"><b><%= schedules[1]%></b></font>
            </div>
          </div>
          <div class="panel-body">
            <div class="top" style="display:flex;height:50px">
              <div class="info">
                <font size="2">작성자 : <%= schedules[6]%></font>
              </div>
              <div class="info">
                <font size="2">이메일 : <%= schedules[7]%></font>
              </div>
              <div class="info">
                <font size="2">시작일 : <%= schedules[4].toString().substr(0,16) %></font>
              </div>
              <div class="info">
                <font size="2">종료일 : <%= schedules[5].toString().substr(0,16) %></font>
              </div>
            </div>
            <div>
              <p style="white-space:pre-line"><%= schedules[2] %></p>
            </div>
          </div>
        </div>
      </div>
      <div class="row" style="margin-top:0px" align="right">
      <% if (iswriter) { %>
        <input type="button" name="add" id="add" value="수정" class="btn btn-primary pull" style="margin-right:5px" onclick="location.href='/schedule/modify/<%= schedules[0] %>'">
        <input type="button" name="add" id="add" value="삭제" class="btn btn-primary pull" style="margin-right:5px" onclick="location.href='/schedule/delete/<%= schedules[0] %>'">
      <% }  %>
        <input type="button" style="margin-right:15px" value="목록" class="btn btn-primary pull" onclick="location.href='/schedule'">
      </div> 
    </div>
  </body>
</html>
