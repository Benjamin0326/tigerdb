<!DOCTYPE html>
<html>
  <head>
    <% include ../partials/head %>
  </head>
  <body>
      <div class="header">
      <% include ../partials/header %>
      </div>
    <div class="container" style="margin-top:30px">
      <div class="list-group">
        <div class="list-group-item active"><h4>포상 기록</h4></div>
        <% if(winners[0]==null){ %>
          <div class="list-group-item" style="height:50px;">
            <p class="list-group-item-text">포상기록이 없습니다.</p>
          </div>
        <% }; %>
        <% winners.forEach(function(winner) { %>
          <div class="list-group-item" style="height:50px;">
            <p class="list-group-item-text col-md-2"><%= winner[1] %></p>
            <p class="list-group-item-text col-md-2"><% if(winner[3]==0){ %>이달의 버그킹<% }else if(winner[3]==1){ %>이달의 사원<% }else if(winner[3]==2){ %>특별 포상<% }; %></p>
            <p class="list-group-item-text col-md-4"><%= winner[6] %>(<%= winner[11] %>)</p>
            <p class="list-group-item-text col-md-2"><%= winner[4].toString().substr(0,16) %></p>
            <p class="list-group-item-text col-md-1"><a href="/people/prize/award/modify/<%= winner[0] %>"><button class="btn btn-primary">수정</button></a></p>
            <p class="list-group-item-text col-md-1"><a href="/people/prize/award/delete/<%= winner[0] %>"><button class="btn btn-primary">삭제</button></a></p>
          </div>
          <% }); %>
      </div>
    </div>

  </body>
</html>
