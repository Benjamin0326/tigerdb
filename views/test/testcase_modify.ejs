<!DOCTYPE html>
<html>
  <head>
    <% include ../partials/head %>
  </head>
  <body>
    <div class="header">
      <% include ../partials/header %>
    </div>
      <div class="container" style="margin-top:30px">
          <div class="row">
              <form role="form" id="body" method="post" action="/test/testcase/modify/commit/<%= testcase[0] %>">
                  <div class="col-lg-12">
                    <div class="form-group">
                          <label for="InputName">Enter Summary</label>
                          <div class="input-group">
                              <input type="text" class="form-control" name="tcsummary" id="InputName" value="<%= testcase[1] %>" required>
                              <span class="input-group-addon"><span class="glyphicon glyphicon-asterisk"></span></span>
                          </div>
                      </div>
                      <div class="form-group">
                        <label for="sel1">Expected Result</label>
                        <select class="form-control" name="tcer" id="sel1">
                            <option <% if(testcase[3]==0){ %> selected="selected" <% }; %> >(0) FAIL</option>
                            <option <% if(testcase[3]==1){ %> selected="selected" <% }; %> >(1) PASS</option>
                        </select>
                      </div>
                      <div class="form-group">
                          <label for="InputMessage">Enter Description</label>
                          <div class="input-group">
                              <textarea name="tcdescription" id="InputMessage" class="form-control" rows="10" required><%= testcase[2] %></textarea>
                              <span class="input-group-addon"><span class="glyphicon glyphicon-asterisk"></span></span>
                          </div>
                      </div>
                      <input type="submit" name="submit" id="submit" value="TestCase 수정" class="btn btn-info pull-right">
                  </div>
              </form>
          </div>
      </div>
    </div>
  </body>
</html>
