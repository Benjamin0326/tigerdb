<!DOCTYPE html>
<html>
  <head>
    <% include partials/head %>
  </head>
  <body>
    <div class="header">
      <% include partials/header %>
    </div>
  <div class="container" style="margin-top:30px">
    <div class="list-group">
        <% phones.forEach(function(phone) { %>
          <div class="list-group-item">
            <h4 class="list-group-item-text"><%= phone[1] %></h4>
            <br>
            <p class="list-group-item-text"><%= phone[2] %></p>
            <p class="list-group-item-text" style="text-algin:right; margin-left:700px"><%= phone[3] %></p>
          </div>
          <% }); %>
      </div>
    </div>
    <div class="conatiner col-md-1">
        <a href="phone/add">스마트폰 추가</a>
      </div>

  </body>
</html>
